---
# Copy to secrets.yml and encrypt with Ansible Vault.
# ansible-vault encrypt provisioning/ansible/hyperv/group_vars/hyperv_hosts/secrets.yml

# WinRM password used by ansible_user from inventory.
ansible_password: "CHANGEME"

# Preferred: dedicated password for the local Hyper-V service account.
hyperv_host_service_user_password: "CHANGEME_SERVICE_USER_PASSWORD"
