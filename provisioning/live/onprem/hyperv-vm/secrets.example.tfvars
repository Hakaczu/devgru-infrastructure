# This file is ignored by Git based on `.gitignore` (*.tfvars)
# Example passwords and addresses (adjust as needed, e.g. encrypt locally with Ansible Vault or SOPS before committing, or just keep them in a password manager and paste them here).

hyperv_host     = "192.168.1.100"
hyperv_user     = "Administrator"
hyperv_password = "SuperSecretPassword123"
